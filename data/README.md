# Data

The dataset used in this project is downloaded directly from Yahoo Finance using the `yfinance` Python package.

Because Yahoo Finance data is publicly available and can change over time, the raw data is not stored in this repository. Running the notebook will download the required data automatically.
